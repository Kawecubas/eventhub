import {redirect,notFound} from "next/navigation";import {isAdmin} from "@/lib/admin-auth";import {getEvent} from "@/lib/event-platform-store";import EventEditor from "./EventEditor";export const dynamic="force-dynamic";export default function Page({params}:{params:{id:string}}){if(!isAdmin())redirect('/admin/login');const e=getEvent(params.id);if(!e)notFound();return <EventEditor initial={e}/>}
