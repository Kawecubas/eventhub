import {NextResponse} from "next/server";import {isAdmin} from "@/lib/admin-auth";import {saveEvent,listEvents} from "@/lib/event-platform-store";export const dynamic="force-dynamic";export async function GET(){if(!isAdmin())return NextResponse.json({error:'Não autorizado'},{status:401});return NextResponse.json(listEvents())}export async function POST(r:Request){if(!isAdmin())return NextResponse.json({error:'Não autorizado'},{status:401});const b=await r.json();if(!b.name||!b.slug)return NextResponse.json({error:'Nome e slug são obrigatórios'},{status:400});return NextResponse.json(saveEvent(b))}
